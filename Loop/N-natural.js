const numbers = parseInt(prompt("enter the natural numbers:"));

let sum = 0;

for (let i = 0; i <= numbers; i++) {
  sum += i;
}

document.write("sum of natural numbers: " + sum);
// const numbers = parseInt(prompt("enter the natural numbers:"));

// let sum = 0;

// for (let i = 0; i <= numbers; i++) {
//   sum += i;
// }

// document.write("sum of natural numbers: " + sum);
